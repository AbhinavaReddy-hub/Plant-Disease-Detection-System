import '../..//styles/invalidCredentials.css';

function InvalidCredentials() {
    return (
        <div className="invalidContainer">
            <p>Invalid Credentials</p>
        </div>
    );
}

export default InvalidCredentials;
